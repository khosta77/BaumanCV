#ifndef CVRANGEFINDER_UCHAR_IN_TYPEDEF_H
#define CVRANGEFINDER_UCHAR_IN_TYPEDEF_H

/*
 * В файле спецефические объявления из библиотеки, а так же все нужные инклуды
 * этот хэдер надо перименнновать 12.08.22
 * */

#include <string>
#include <vector>

typedef std::string String;
typedef std::pair<uint32_t, uint32_t> u_rational_t;

using uchar = unsigned char;
using schar = signed char;
using ushort = unsigned short;
using uint = unsigned int;
using uint64 = unsigned long int;
using int64 = long int;

#endif //CVRANGEFINDER_UCHAR_IN_TYPEDEF_H
